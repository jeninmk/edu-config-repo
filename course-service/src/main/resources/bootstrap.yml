spring:
  application:
    name: course-service      # or course-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev              # pick dev or prod
      fail-fast: true
