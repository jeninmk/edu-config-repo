spring:
  application:
    name: progress-service
  datasource:
    url: ${DATASOURCE_URL:jdbc:mysql://mysql:3306/progressdb}
    driver-class-name: ${DATASOURCE_DRIVER:com.mysql.cj.jdbc.Driver}
    username: ${DATASOURCE_USERNAME:progress}
    password: ${DATASOURCE_PASSWORD:progress123}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}
    fetch-registry: true
    register-with-eureka: true
logging:
  level:
    org.springframework: INFO
